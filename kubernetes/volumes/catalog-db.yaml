apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-db-data
  labels:
    app.kubernetes.io/name: catalog-db-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi