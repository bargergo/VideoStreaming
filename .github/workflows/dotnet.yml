name: .NET

on:
  push:
    branches: [ master ]
    paths-ignore:
      - 'VideoStreaming/CatalogService/**'
      - 'VideoStreaming/UploadService/**'
      - 'webclient/**'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - 'VideoStreaming/CatalogService/**'
      - 'VideoStreaming/UploadService/**'
      - 'webclient/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - uses: actions/cache@v2
      with:
        path: |
          ~/.nuget/packages
          !~/.nuget/packages/unwanted
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    - name: Restore dependencies
      run: cd VideoStreaming/ConvertService && dotnet restore
    - name: Build
      run: cd VideoStreaming/ConvertService && dotnet build --configuration Release --no-restore
    - name: Test
      run: cd VideoStreaming/ConvertService && dotnet test --no-build --verbosity normal
